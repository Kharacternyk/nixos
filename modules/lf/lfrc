set dircounts
set dupfilefmt "%b-%n%e"
set errorfmt "\033[7;31m%s\033[0m"
set hidden
set incsearch
set info size
set scrolloff 100

map q
map D delete "$fx"
map t $difft --color always $fs | less -R

cmd q quit
cmd open $rifle "$f"
