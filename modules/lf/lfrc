set hidden
set incsearch
set dircounts
set info size
set scrolloff 100
set errorfmt "\033[7;31m%s\033[0m"
set dupfilefmt "%b-%n%e"

map q
map D delete "$fx"
map t $difft --color always $fs | less -R

cmd q quit
cmd open $rifle "$f"
